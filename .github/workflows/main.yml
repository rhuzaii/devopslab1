name: Greeting Workflow

on:
  push:
    branches:
      - main

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print Greeting
        run: echo "Hello Rana! 🚀 CI is running."

      - name: Print Current Date
        run: date

      - name: Print Current Directory
        run: pwd
